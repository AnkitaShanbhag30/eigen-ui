<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title or "Personalized Highlights" }}</title>
    
    <!-- Dynamic Font Loading -->
    {% if brand.fonts_detected %}
        {% for font in brand.fonts_detected[:2] %}
            <link href="https://fonts.googleapis.com/css2?family={{ font|replace(' ', '+') }}:wght@300;400;500;600;700&display=swap" rel="stylesheet">
        {% endfor %}
    {% else %}
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    {% endif %}
    
    <style>
        :root {
            /* Dynamic Color Variables from Theme */
            --bg: {{ tokens.colors.bg or '#ffffff' }};
            --surface: {{ tokens.colors.surface or '#f8f9fa' }};
            --text: {{ tokens.colors.text or '#0B0B0B' }};
            --text-muted: {{ tokens.colors.text_muted or '#6c757d' }};
            --brand-500: {{ tokens.colors.primary }};
            --brand-700: {{ tokens.colors.primary_dark or tokens.colors.primary }};
            --accent-500: {{ tokens.colors.secondary }};
            --accent-700: {{ tokens.colors.secondary_dark or tokens.colors.secondary }};
            --muted: {{ tokens.colors.muted or '#e9ecef' }};
            --cta-bg: {{ tokens.colors.primary }};
            --cta-fg: #ffffff;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Inter', system-ui, sans-serif;
            line-height: 1.4;
            color: var(--text);
            background-color: var(--surface);
            font-size: 16px;
            overflow: hidden;
        }
        
        /* Story Container - 9:16 Aspect Ratio */
        .story-container {
            width: 1080px;
            height: 1920px;
            margin: 0 auto;
            position: relative;
            background: linear-gradient(135deg, var(--surface), var(--muted));
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            padding: 96px;
        }
        
        /* Header Section */
        .story-header {
            text-align: left;
            margin-bottom: 64px;
        }
        
        .story-header h1 {
            font-size: 4.5rem;
            font-weight: 700;
            line-height: 1.1;
            color: var(--brand-500);
            margin-bottom: 32px;
            max-width: 80%;
        }
        
        .story-header p {
            font-size: 2rem;
            color: var(--text-muted);
            max-width: 90%;
            line-height: 1.3;
        }
        
        /* Hero Image Section */
        .hero-section {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 32px 0;
        }
        
        .hero-visual {
            width: 100%;
            height: 60%;
            background: linear-gradient(135deg, var(--bg), var(--surface));
            border-radius: 32px;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
        }
        
        .hero-visual::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(45deg, var(--brand-500), var(--accent-500));
            opacity: 0.08;
        }
        
        .hero-content {
            position: relative;
            z-index: 1;
            text-align: center;
            color: var(--brand-700);
        }
        
        .hero-content h3 {
            font-size: 2.5rem;
            margin-bottom: 24px;
            font-weight: 600;
        }
        
        .hero-content p {
            font-size: 1.5rem;
            opacity: 0.8;
        }
        
        /* Personalization Highlights */
        .highlights-section {
            position: absolute;
            top: 50%;
            right: 48px;
            transform: translateY(-50%);
            display: flex;
            flex-direction: column;
            gap: 16px;
        }
        
        .highlight-chip {
            background-color: var(--brand-500);
            color: var(--cta-fg);
            padding: 12px 20px;
            border-radius: 20px;
            font-size: 1rem;
            font-weight: 500;
            white-space: nowrap;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }
        
        .highlight-chip:nth-child(2) {
            background-color: var(--accent-500);
        }
        
        .highlight-chip:nth-child(3) {
            background-color: var(--muted);
            color: var(--brand-700);
        }
        
        /* CTA Section */
        .cta-section {
            text-align: center;
            margin-top: 32px;
        }
        
        .cta-button {
            display: inline-block;
            background-color: var(--cta-bg);
            color: var(--cta-fg);
            padding: 24px 48px;
            border-radius: 20px;
            text-decoration: none;
            font-weight: 600;
            font-size: 1.75rem;
            transition: all 0.2s ease;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
        }
        
        .cta-button:hover {
            background-color: var(--brand-700);
            transform: translateY(-2px);
            box-shadow: 0 12px 35px rgba(0, 0, 0, 0.2);
        }
        
        /* Footer Section */
        .story-footer {
            text-align: center;
            margin-top: 32px;
        }
        
        .brand-name {
            font-size: 1.5rem;
            color: var(--text-muted);
            opacity: 0.75;
            font-weight: 500;
        }
        
        /* Data Visualization */
        .data-viz {
            position: absolute;
            bottom: 200px;
            left: 48px;
            display: flex;
            flex-direction: column;
            gap: 12px;
        }
        
        .data-point {
            background-color: var(--accent-500);
            color: var(--cta-fg);
            padding: 8px 16px;
            border-radius: 16px;
            font-size: 0.9rem;
            font-weight: 500;
            text-align: center;
        }
        
        .data-point:nth-child(2) {
            background-color: var(--brand-500);
        }
        
        .data-point:nth-child(3) {
            background-color: var(--muted);
            color: var(--brand-700);
        }
    </style>
</head>
<body>
    <div class="story-container">
        <!-- Header Section -->
        <div class="story-header">
            <h1>{{ outline.headline or "Personalized Highlights" }}</h1>
            <p>{{ outline.subhead or "Dynamic content that adapts to your audience with 1P + 3P data for maximum relevance." }}</p>
        </div>
        
        <!-- Hero Section -->
        <div class="hero-section">
            <div class="hero-visual">
                <div class="hero-content">
                    <h3>{{ brand.name or "Brand" }}</h3>
                    <p>AI-Powered Personalization</p>
                </div>
            </div>
        </div>
        
        <!-- Personalization Highlights -->
        <div class="highlights-section">
            <div class="highlight-chip">1P Data</div>
            <div class="highlight-chip">3P Context</div>
            <div class="highlight-chip">Real-time</div>
        </div>
        
        <!-- Data Visualization -->
        <div class="data-viz">
            <div class="data-point">Klaviyo</div>
            <div class="data-point">Quiz Data</div>
            <div class="data-point">Location</div>
        </div>
        
        <!-- CTA Section -->
        <div class="cta-section">
            <a href="#" class="cta-button">{{ outline.cta or "Learn More" }}</a>
        </div>
        
        <!-- Footer -->
        <div class="story-footer">
            <div class="brand-name">{{ brand.name or "Brand" }}</div>
        </div>
    </div>
</body>
</html>
